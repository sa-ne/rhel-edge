apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    endpoint: edge
  name: edge-service-monitor
  namespace: edge-rhel-endpoints
spec:
  endpoints:
  - interval: 20s
    port: 44323-tcp
    scheme: http
  selector:
    matchLabels:
      endpoint: edge
